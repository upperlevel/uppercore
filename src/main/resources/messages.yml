commands:
  usage:
    command:
       executable: "&b%name%"
       denied: "&c%name%"
    argument: "&3%name%"
  help:
    #[header]
    #[usage]
    #[usage]
    #...
    #[footer]
    header: #[init][arrow.previous][pages][arrow.next][end]
      init: "&6\"%command_name% Help "
      arrow:
        previous:
          enabled: "&a[<]"
          disabled: "&c[<]"
        next:
          enabled: "&a[>]"
          disabled: "&c[>]"
      pages: " &6%page%/%pages% "
      end: ":"
    usage:
      command:
         executable: "&b%name%"
         denied: "&c%name%"
      argument: "&3%name%"
      description: "&7%description%"
    footer: ""
